"# Syntecxhub__Data_cleaning_utility" 

Data cleaning utility

Detect and handle missing values (drop / fill /
impute).
Fix incorrect dtypes (dates, numbers) and parse
dates.
Remove duplicates and standardize column
names.
Output a cleaned dataset and brief cleaning
log.
